# TODO - Industrial Luxury/Web3 Minimal home design system

- [x] Update `frontend/src/styles.scss` with global design tokens (colors, fonts, cubic-bezier), noise/grain overlay, and radial dot grid background utilities.

- [x] Expand `frontend/src/app/pages/home/home.component.scss` to fully match spec (hero layers, mission padding, card styles/badges, footer typography, glass button glow, section backgrounds, reveal/parallax polish).

- [ ] Ensure only the specified easing curve is used for transitions/animations (replace any other ease).
- [ ] Run Angular build/serve command to confirm compilation.

